#pragma warning disable CA1716
namespace WhatsBiz.Application.Common.Interfaces;
public sealed record PartyLedgerRow(DateTimeOffset EntryDate,string EntryType,string? ReferenceNumber,decimal Debit,decimal Credit,decimal Balance,string? Narration);public sealed record BookRow(DateTimeOffset EntryDate,string EntryType,string? ReferenceNumber,string? Mode,decimal AmountIn,decimal AmountOut,decimal Balance,string? Narration);public sealed record DayBookRow(DateTimeOffset EntryDate,string TransactionType,string? ReferenceNumber,decimal DebitTotal,decimal CreditTotal,string? Narration);public sealed record FinanceSummary(decimal TotalIn,decimal TotalOut,decimal Balance);
public interface IFinanceRepository{Task<IReadOnlyCollection<PartyLedgerRow>> CustomerLedger(Guid id,DateTimeOffset? from,DateTimeOffset? to,CancellationToken token);Task<IReadOnlyCollection<PartyLedgerRow>> SupplierLedger(Guid id,DateTimeOffset? from,DateTimeOffset? to,CancellationToken token);Task<IReadOnlyCollection<BookRow>> CashBook(DateTimeOffset? from,DateTimeOffset? to,CancellationToken token);Task<IReadOnlyCollection<BookRow>> BankBook(DateTimeOffset? from,DateTimeOffset? to,CancellationToken token);Task<IReadOnlyCollection<DayBookRow>> DayBook(DateTimeOffset? from,DateTimeOffset? to,CancellationToken token);Task<(Guid Id,string Number)> Post(string transactionType,string partyType,Guid partyId,string mode,decimal amount,DateTimeOffset date,string? reference,string? narration,string? user,CancellationToken token);}
