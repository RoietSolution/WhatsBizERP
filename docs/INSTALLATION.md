# Installation

Install .NET SDK 9, Node.js 22, npm, Docker Desktop, and SQL Server 2022. Restore the backend with `dotnet restore backend/WhatsBiz.sln`, build with `dotnet build backend/WhatsBiz.sln --no-restore`, and run the web app from `frontend/WhatsBiz.Web` using `npm start`.
