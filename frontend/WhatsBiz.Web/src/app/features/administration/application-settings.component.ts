import{Component,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{AdminApiService,Setting}from'./admin-api.service';@Component({imports:[FormsModule,MatButtonModule],template:`<h1>{{title}}</h1><section>@for(group of categories();track group){<article><h2>{{group}}</h2>@for(x of byCategory(group);track x.key){<label>{{x.key}}<input [(ngModel)]="x.value"></label>}</article>}</section><button mat-flat-button (click)="save()">Save settings</button>`,styles:[`section{display:grid;grid-template-columns:repeat(2,1fr);gap:1rem}article{background:#fff;padding:1rem}label{display:grid;margin:.7rem 0}input{padding:.6rem}@media(max-width:700px){section{grid-template-columns:1fr}}`]})export class ApplicationSettingsComponent{items=signal<Setting[]>([]);title='Application Settings';constructor(private api:AdminApiService,route:ActivatedRoute){this.title=route.snapshot.data['title']??this.title;api.settings().subscribe(x=>this.items.set(x))}categories(){return[...new Set(this.items().map(x=>x.category))]}byCategory(x:string){return this.items().filter(y=>y.category===x)}save(){this.api.saveSettings(this.items()).subscribe()}}
