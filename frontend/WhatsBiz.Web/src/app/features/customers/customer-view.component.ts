import{ChangeDetectionStrategy,Component,signal}from'@angular/core';import{ActivatedRoute,RouterLink}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{MatSnackBar}from'@angular/material/snack-bar';import{MatTabsModule}from'@angular/material/tabs';import{CustomerApiService}from'./customer-api.service';import{Customer}from'./customer.models';
@Component({imports:[RouterLink,MatButtonModule,MatTabsModule],template:`@if(customer();as c){<header><div><h1>{{c.customerName}}</h1><p>{{c.customerCode}} · {{c.isActive?'Active':'Inactive'}}</p></div><a mat-flat-button [routerLink]="['/customers',c.customerId,'edit']">Edit</a></header><mat-tab-group><mat-tab label="General"><dl><dt>Type</dt><dd>{{c.customerType}}</dd><dt>GSTIN</dt><dd>{{c.gstin||'—'}}</dd><dt>PAN</dt><dd>{{c.pan||'—'}}</dd><dt>Email</dt><dd>{{c.email||'—'}}</dd><dt>Mobile</dt><dd>{{c.mobile||'—'}}</dd><dt>Payment terms</dt><dd>{{c.paymentTermName||'—'}}</dd><dt>Credit limit</dt><dd>{{c.creditLimit}}</dd></dl></mat-tab><mat-tab label="Contacts">@for(x of c.contacts;track x.contactId){<p><strong>{{x.contactPerson}}</strong> {{x.designation}} · {{x.mobile}} · {{x.email}}</p>}</mat-tab><mat-tab label="Addresses">@for(x of c.addresses;track x.addressId){<p>{{x.addressType}}: {{x.addressLine1}}, {{x.city}}, {{x.state}} {{x.postalCode}}</p>}</mat-tab><mat-tab label="Bank Accounts">@for(x of c.bankAccounts;track x.bankAccountId){<p>{{x.bankName}} · {{x.accountNumber}} · {{x.ifscCode}}</p>}</mat-tab><mat-tab label="Documents"><div class="upload"><select #type><option>GST Certificate</option><option>PAN</option><option>Agreement</option><option>Other</option></select><input #file type="file"><button mat-button (click)="upload(type.value,file)">Upload</button></div>@for(x of c.documents;track x.documentId){<p>{{x.documentType}}: <button mat-button (click)="preview(x.documentId)">{{x.fileName}}</button><button mat-button (click)="replace(type.value,file,x.documentId)">Replace</button><button mat-button (click)="remove(x.documentId)">Delete</button></p>}</mat-tab></mat-tab-group>}`,styles:[`header{display:flex;justify-content:space-between}mat-tab-group{margin-top:1rem}dl{display:grid;grid-template-columns:150px 1fr;gap:1rem;padding:1rem}dd{margin:0}.upload{display:flex;gap:1rem;padding:1rem;flex-wrap:wrap}`],changeDetection:ChangeDetectionStrategy.OnPush})export class CustomerViewComponent{readonly customer=signal<Customer|null>(null);private readonly id:string;constructor(private readonly api:CustomerApiService,route:ActivatedRoute,private readonly snack:MatSnackBar){this.id=route.snapshot.paramMap.get('id')??'';this.load()}load(){this.api.get(this.id).subscribe(x=>this.customer.set(x))}upload(type:string,input:HTMLInputElement){const f=input.files?.[0];if(f)this.api.upload(this.id,type,f).subscribe(()=>{input.value='';this.snack.open('Document uploaded.',undefined,{duration:2500});this.load()})}replace(type:string,input:HTMLInputElement,did:string){const f=input.files?.[0];if(f)this.api.deleteDocument(this.id,did).subscribe(()=>this.upload(type,input))}preview(did:string){this.api.document(this.id,did).subscribe(b=>{const u=URL.createObjectURL(b);window.open(u,'_blank');setTimeout(()=>URL.revokeObjectURL(u),60000)})}remove(did:string){this.api.deleteDocument(this.id,did).subscribe(()=>{this.snack.open('Document deleted.',undefined,{duration:2500});this.load()})}}
