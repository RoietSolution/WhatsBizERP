import { CommonModule } from '@angular/common';import { ChangeDetectionStrategy,Component,inject,signal } from '@angular/core';import { FormsModule } from '@angular/forms';import { MatButtonModule } from '@angular/material/button';import { DeliveryAgent,DeliveryApiService } from './delivery-api.service';
@Component({imports:[CommonModule,FormsModule,MatButtonModule],template:`<section class="page"><header><div><small>Administration</small><h1>Delivery Agents</h1><p>Agents authenticate as existing tenant users.</p></div><a mat-stroked-button href="/admin/delivery-settings">Settings</a></header><form (ngSubmit)="create()"><input [(ngModel)]="draft.userId" name="user" placeholder="Existing user ID" required><input [(ngModel)]="draft.displayName" name="name" placeholder="Display name" required><input [(ngModel)]="draft.mobile" name="mobile" placeholder="Mobile" required><button mat-flat-button color="primary">Add agent</button></form><p class="message">{{message()}}</p><div class="agents"><article *ngFor="let a of agents()"><div><strong>{{a.displayName}} <span *ngIf="a.isDefault">Default</span></strong><small>{{a.mobile}}</small></div><label><input type="checkbox" [ngModel]="a.isActive" (ngModelChange)="update(a,{isActive:$event})"> Active</label><label><input type="checkbox" [ngModel]="a.isAvailable" (ngModelChange)="update(a,{isAvailable:$event})"> Available</label><div><b>{{a.activeDeliveries}}</b><small>Active</small></div><div><b>{{a.deliveredToday}}</b><small>Today</small></div><button mat-stroked-button *ngIf="!a.isDefault&&a.isActive" (click)="default(a)">Set default</button><button mat-stroked-button *ngIf="a.isDefault" (click)="removeDefault()">Remove default</button></article></div></section>`,styles:[`.page{padding:20px}header{display:flex;justify-content:space-between}h1{margin:2px 0}header p,small{color:#667085}form{display:flex;gap:8px;flex-wrap:wrap;margin:18px 0}input{min-height:40px;border:1px solid #cbd5e1;border-radius:8px;padding:0 10px}.agents{display:grid;gap:8px}.agents article{display:grid;grid-template-columns:2fr 1fr 1fr .6fr .6fr auto;align-items:center;gap:12px;padding:14px;background:white;border:1px solid #dde4eb;border-radius:12px}.agents strong,.agents small{display:block}.agents span{font-size:.7rem;background:#dff3e9;color:#08634f;padding:3px 6px;border-radius:10px}.message{color:#8a4b08}@media(max-width:760px){.agents article{grid-template-columns:1fr 1fr}.agents article>div:first-child{grid-column:1/-1}.page{padding:12px}}`],changeDetection:ChangeDetectionStrategy.OnPush})
export class DeliveryAgentsComponent{private readonly api=inject(DeliveryApiService);readonly agents=signal<DeliveryAgent[]>([]);readonly message=signal('');draft={userId:'',displayName:'',mobile:''};constructor(){this.load()}load(){this.api.agents().subscribe(x=>this.agents.set(x))}create(){this.api.saveAgent(this.draft).subscribe({next:()=>{this.draft={userId:'',displayName:'',mobile:''};this.load()},error:e=>this.message.set(e.error?.detail||'Unable to add agent.')})}update(a:DeliveryAgent,patch:Partial<DeliveryAgent>){this.api.saveAgent({...a,...patch},a.deliveryAgentId).subscribe({next:()=>this.load(),error:e=>this.message.set(e.error?.detail||'Update failed.')})}default(a:DeliveryAgent){this.api.setDefault(a.deliveryAgentId).subscribe(()=>this.load())}removeDefault(){this.api.setDefault().subscribe(()=>this.load())}}
