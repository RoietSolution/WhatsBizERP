import{ChangeDetectionStrategy,Component,computed,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute,RouterLink}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{MatFormFieldModule}from'@angular/material/form-field';import{MatSelectModule}from'@angular/material/select';import{OperationsWorkspaceComponent}from'../../shared/components/operations-workspace/operations-workspace.component';import{FilterPanelComponent}from'../../shared/components/filter-panel/filter-panel.component';import{DataTableComponent}from'../../shared/components/data-table/data-table.component';import{FinanceApiService,LedgerRow}from'./finance-api.service';
@Component({imports:[FormsModule,RouterLink,MatButtonModule,MatFormFieldModule,MatSelectModule,OperationsWorkspaceComponent,FilterPanelComponent,DataTableComponent],template:`<app-operations-workspace eyebrow="Finance command center" [title]="(party==='customer'?'Customer':'Supplier')+' Ledger'" description="Review opening balance, vouchers, running balance, and closing position." [summaries]="summaries()" [statusText]="rows().length+' ledger entries'"><a workspace-header-actions mat-flat-button color="primary" [routerLink]="party==='customer'?'/finance/receipt':'/finance/payment'"><span class="material-symbols-rounded">add</span>{{party==='customer'?'New Receipt':'New Payment'}}</a><div workspace-toolbar-actions class="actions"><button mat-button (click)="load()"><span class="material-symbols-rounded">refresh</span>Refresh</button><button mat-button (click)="print()"><span class="material-symbols-rounded">print</span>Print Ledger</button><button mat-button (click)="exportCsv()"><span class="material-symbols-rounded">download</span>Export Ledger</button></div><app-filter-panel workspace-filters title="Ledger filters" (apply)="load()"><div class="filters"><mat-form-field appearance="outline"><mat-label>Select {{party}}</mat-label><mat-select [(ngModel)]="id">@for(x of parties();track party==='customer'?x.customerId:x.supplierId){<mat-option [value]="party==='customer'?x.customerId:x.supplierId">{{party==='customer'?x.customerName:x.supplierName}}</mat-option>}</mat-select></mat-form-field><button mat-stroked-button>Date Range</button><button mat-stroked-button>Branch</button><button mat-stroked-button>Voucher Type</button></div></app-filter-panel><app-data-table [rows]="rows()" [columns]="columns" searchPlaceholder="Search voucher or reference" (selectionChange)="selected.set($event[0]??null)"/><section workspace-context class="insight"><h3>Selected Ledger Summary</h3><dl><dt>Opening balance</dt><dd>{{opening()}}</dd><dt>Closing balance</dt><dd>{{balance()}}</dd><dt>Total debit</dt><dd>{{debit()}}</dd><dt>Total credit</dt><dd>{{credit()}}</dd></dl></section><section workspace-context class="insight"><h3>Voucher Insight</h3>@if(selected();as x){<strong>{{x.referenceNumber||'Voucher'}}</strong><p>{{x.entryType}} · Balance {{x.balance}}</p><button mat-stroked-button>View Voucher</button>}@else{<p>Select a ledger transaction to view notes, attachments, and audit trail.</p>}</section></app-operations-workspace>`,styles:[`.actions,.filters{display:flex;align-items:center;flex-wrap:wrap;gap:6px}.filters mat-form-field{min-width:280px}.insight{padding:18px;background:var(--wb-surface);border:1px solid var(--wb-border);border-radius:var(--wb-radius-md)}.insight h3{margin-top:0}.insight p{color:var(--wb-text-secondary)}dl{display:grid;grid-template-columns:1fr auto;gap:10px}dd{margin:0;font-weight:700}`],changeDetection:ChangeDetectionStrategy.OnPush})export class PartyLedgerComponent{party='customer';id='';readonly parties=signal<any[]>([]);readonly rows=signal<LedgerRow[]>([]);readonly selected=signal<LedgerRow|null>(null);readonly opening=computed(()=>this.rows()[0]?.balance??0);readonly balance=computed(()=>this.rows().at(-1)?.balance??0);readonly debit=computed(()=>this.rows().reduce((a,x)=>a+x.debit,0));readonly credit=computed(()=>this.rows().reduce((a,x)=>a+x.credit,0));readonly summaries=computed(()=>[{label:'Opening balance',value:this.opening(),subtitle:'Period opening',icon:'first_page',tone:'info' as const},{label:'Total debit',value:this.debit(),subtitle:'Ledger debit',icon:'south_west',tone:'success' as const},{label:'Total credit',value:this.credit(),subtitle:'Ledger credit',icon:'north_east',tone:'warning' as const},{label:'Closing balance',value:this.balance(),subtitle:'Running balance',icon:'account_balance_wallet',tone:'primary' as const}]);readonly columns=[{field:'entryDate',headerName:'Date',valueFormatter:(p:any)=>String(p.value??'').slice(0,10)},{field:'entryType',headerName:'Voucher type'},{field:'referenceNumber',headerName:'Voucher / Reference',minWidth:200},{field:'debit',headerName:'Debit'},{field:'credit',headerName:'Credit'},{field:'balance',headerName:'Running balance'}];constructor(private api:FinanceApiService,route:ActivatedRoute){this.party=route.snapshot.data['party'];(this.party==='customer'?api.customers():api.suppliers()).subscribe(x=>this.parties.set(x))}load(){if(!this.id)return;(this.party==='customer'?this.api.customer(this.id):this.api.supplier(this.id)).subscribe(x=>this.rows.set(x))}print(){window.print()}exportCsv(){const data=this.rows();if(!data.length)return;const keys=Object.keys(data[0]),csv=[keys.join(','),...data.map((x:any)=>keys.map(k=>`"${String(x[k]??'').replaceAll('"','""')}"`).join(','))].join('\n'),a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download=`${this.party}-ledger.csv`;a.click()}}
