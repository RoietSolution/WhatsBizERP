import{Component,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute}from'@angular/router';import{MatFormFieldModule}from'@angular/material/form-field';import{MatSelectModule}from'@angular/material/select';import{FinanceApiService,LedgerRow}from'./finance-api.service';@Component({imports:[FormsModule,MatFormFieldModule,MatSelectModule],template:`<h1>{{party==='customer'?'Customer':'Supplier'}} Ledger</h1><mat-form-field><mat-label>Select {{party}}</mat-label><mat-select [(ngModel)]="id" (selectionChange)="load()">@for(x of parties();track party==='customer'?x.customerId:x.supplierId){<mat-option [value]="party==='customer'?x.customerId:x.supplierId">{{party==='customer'?x.customerName:x.supplierName}}</mat-option>}</mat-select></mat-form-field><div class="summary">Outstanding: <strong>{{balance()}}</strong></div><div class="table"><table><tr><th>Date</th><th>Type</th><th>Reference</th><th>Debit</th><th>Credit</th><th>Balance</th></tr>@for(x of rows();track $index){<tr><td>{{date(x.entryDate)}}</td><td>{{x.entryType}}</td><td>{{x.referenceNumber}}</td><td>{{x.debit}}</td><td>{{x.credit}}</td><td>{{x.balance}}</td></tr>}</table></div>`,styles:[`mat-form-field{width:360px}.summary{float:right;font-size:1.2rem}.table{overflow:auto}table{width:100%;border-collapse:collapse}th,td{padding:.7rem;border-bottom:1px solid #ddd;text-align:left}`]})export class PartyLedgerComponent{party='customer';id='';parties=signal<any[]>([]);rows=signal<LedgerRow[]>([]);constructor(private api:FinanceApiService,route:ActivatedRoute){this.party=route.snapshot.data['party'];(this.party==='customer'?api.customers():api.suppliers()).subscribe(x=>this.parties.set(x))}load(){(this.party==='customer'?this.api.customer(this.id):this.api.supplier(this.id)).subscribe(x=>this.rows.set(x))}balance(){return this.rows().at(-1)?.balance??0}date(x:string){return x.substring(0,10)}}
