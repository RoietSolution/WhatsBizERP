import{CurrencyPipe,DatePipe,DecimalPipe}from'@angular/common';import{Component,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{MatProgressSpinnerModule}from'@angular/material/progress-spinner';import{MatSnackBar,MatSnackBarModule}from'@angular/material/snack-bar';import{finalize}from'rxjs';import{GstApiService,GstFilter,GstRow}from'./gst-api.service';
@Component({imports:[CurrencyPipe,DatePipe,DecimalPipe,FormsModule,MatButtonModule,MatProgressSpinnerModule,MatSnackBarModule],template:`<header><div><h1>{{title}}</h1><p>Statutory GST figures sourced from posted sales and purchases.</p></div><nav><button mat-stroked-button (click)="download('xlsx')">Excel</button><button mat-stroked-button (click)="download('pdf')">PDF</button><button mat-stroked-button (click)="download('csv')">CSV</button></nav></header><section class="filters"><label>From<input type="date" [(ngModel)]="from"></label><label>To<input type="date" [(ngModel)]="to"></label><label>GST Rate<input type="number" min="0" max="100" [(ngModel)]="gstRate"></label><button mat-flat-button (click)="load()">Apply filters</button></section><section class="cards"><article><span>Taxable Value</span><b>{{total('taxableAmount')|currency:'INR'}}</b></article><article><span>CGST + SGST</span><b>{{total('cgstAmount')+total('sgstAmount')|currency:'INR'}}</b></article><article><span>IGST</span><b>{{total('igstAmount')|currency:'INR'}}</b></article><article><span>Total GST</span><b>{{total('totalTax')|currency:'INR'}}</b></article></section>@if(loading()){<mat-spinner diameter="42"/>}@else{<div class="table"><table><thead><tr><th>Document / Type</th><th>Date</th><th>Party / GSTIN</th><th>HSN/SAC</th><th>Rate</th><th>Qty</th><th>Taxable</th><th>CGST</th><th>SGST</th><th>IGST</th><th>Tax</th></tr></thead><tbody>@for(r of rows();track $index){<tr><td>{{r.documentNumber||r.supplyType||r.sourceType}}</td><td>{{r.documentDate|date:'dd MMM yyyy'}}</td><td>{{r.partyName}}<small>{{r.partyGstin}}</small></td><td>{{r.hsnCode}}</td><td>{{r.gstRate|number:'1.0-2'}}%</td><td>{{r.quantity|number:'1.0-3'}}</td><td>{{r.taxableAmount|number:'1.2-2'}}</td><td>{{r.cgstAmount|number:'1.2-2'}}</td><td>{{r.sgstAmount|number:'1.2-2'}}</td><td>{{r.igstAmount|number:'1.2-2'}}</td><td>{{r.totalTax|number:'1.2-2'}}</td></tr>}@empty{<tr><td colspan="11">No GST transactions for this period.</td></tr>}</tbody></table></div>}`,styles:[`header{display:flex;justify-content:space-between}header p,small{color:#657080}nav,.filters{display:flex;gap:.6rem;flex-wrap:wrap}.filters{padding:1rem;background:#fff;border:1px solid #e1e5eb;border-radius:.6rem;align-items:end}.filters label{display:grid}.filters input{padding:.55rem}.cards{display:grid;grid-template-columns:repeat(4,1fr);gap:1rem;margin:1rem 0}.cards article{padding:1rem;background:#fff;border:1px solid #dce3ec;border-radius:.6rem}.cards span,small{display:block}.cards b{font-size:1.2rem}.table{overflow:auto;background:#fff}table{border-collapse:collapse;width:100%;min-width:1050px}th,td{text-align:left;padding:.7rem;border-bottom:1px solid #edf0f4}th{background:#f5f7fa}mat-spinner{margin:2rem auto}@media(max-width:800px){header{display:block}.cards{grid-template-columns:repeat(2,1fr)}}`]})export class GstReportComponent{title='GST Report';report='tax-summary';from=new Date(new Date().getFullYear(),new Date().getMonth(),1).toISOString().slice(0,10);to=new Date().toISOString().slice(0,10);gstRate?:number;readonly rows=signal<GstRow[]>([]);readonly loading=signal(false);constructor(private api:GstApiService,route:ActivatedRoute,private snack:MatSnackBar){this.title=route.snapshot.data['title'];this.report=route.snapshot.data['report'];this.load();}filter():GstFilter{return{from:this.from,to:this.to,gstRate:this.gstRate}}load(){this.loading.set(true);this.api.report(this.report,this.filter()).pipe(finalize(()=>this.loading.set(false))).subscribe({next:x=>this.rows.set(x),error:()=>this.snack.open('Unable to load GST report','Close',{duration:3500})});}total(f:keyof GstRow){return this.rows().reduce((n,r)=>n+(Number(r[f])||0),0)}download(format:string){this.api.export(this.report,format,this.filter()).subscribe({next:b=>{const a=document.createElement('a');a.href=URL.createObjectURL(b);a.download=`${this.report}.${format}`;a.click();URL.revokeObjectURL(a.href)},error:()=>this.snack.open('Export failed','Close',{duration:3500})})}}
