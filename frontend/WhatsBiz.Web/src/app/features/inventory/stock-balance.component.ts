import{ChangeDetectionStrategy,Component,signal}from'@angular/core';import{FormControl,ReactiveFormsModule}from'@angular/forms';import{MatButtonModule}from'@angular/material/button';import{MatFormFieldModule}from'@angular/material/form-field';import{MatInputModule}from'@angular/material/input';import{MatPaginatorModule,PageEvent}from'@angular/material/paginator';import{MatSelectModule}from'@angular/material/select';import{MatTableModule}from'@angular/material/table';import{InventoryApiService}from'./inventory-api.service';import{Balance,ProductOption,WarehouseOption}from'./inventory.models';
@Component({imports:[ReactiveFormsModule,MatButtonModule,MatFormFieldModule,MatInputModule,MatPaginatorModule,MatSelectModule,MatTableModule],template:`<h1>Stock Balance</h1><div class="tools"><mat-form-field><mat-label>Search</mat-label><input matInput [formControl]="search" (keyup.enter)="load()"></mat-form-field><mat-form-field><mat-label>Warehouse</mat-label><mat-select [formControl]="warehouse" (selectionChange)="load()"><mat-option value="">All</mat-option>@for(x of warehouses();track x.warehouseId){<mat-option [value]="x.warehouseId">{{x.warehouseName}}</mat-option>}</mat-select></mat-form-field><mat-form-field><mat-label>Product</mat-label><mat-select [formControl]="product" (selectionChange)="load()"><mat-option value="">All</mat-option>@for(x of products();track x.productId){<mat-option [value]="x.productId">{{x.productCode}} — {{x.productName}}</mat-option>}</mat-select></mat-form-field><button mat-button (click)="export()">Export Excel</button></div>@if(loading()){<p>Loading stock...</p>}<div class="table"><table mat-table [dataSource]="items()"><ng-container matColumnDef="product"><th mat-header-cell *matHeaderCellDef>Product</th><td mat-cell *matCellDef="let x">{{x.productCode}} — {{x.productName}}</td></ng-container><ng-container matColumnDef="warehouse"><th mat-header-cell *matHeaderCellDef>Warehouse</th><td mat-cell *matCellDef="let x">{{x.warehouseName}}</td></ng-container><ng-container matColumnDef="location"><th mat-header-cell *matHeaderCellDef>Location</th><td mat-cell *matCellDef="let x">{{x.zoneCode||'—'}} / {{x.binCode||'—'}}</td></ng-container><ng-container matColumnDef="onHand"><th mat-header-cell *matHeaderCellDef>On Hand</th><td mat-cell *matCellDef="let x">{{x.quantityOnHand}}</td></ng-container><ng-container matColumnDef="reserved"><th mat-header-cell *matHeaderCellDef>Reserved</th><td mat-cell *matCellDef="let x">{{x.quantityReserved}}</td></ng-container><ng-container matColumnDef="available"><th mat-header-cell *matHeaderCellDef>Available</th><td mat-cell *matCellDef="let x">{{x.quantityAvailable}}</td></ng-container><ng-container matColumnDef="value"><th mat-header-cell *matHeaderCellDef>Stock Value</th><td mat-cell *matCellDef="let x">{{x.stockValue}}</td></ng-container><tr mat-header-row *matHeaderRowDef="cols"></tr><tr mat-row *matRowDef="let row;columns:cols"></tr></table></div><mat-paginator [length]="total()" [pageIndex]="page-1" [pageSize]="size" (page)="paged($event)"/>`,styles:[`.tools{display:flex;gap:1rem;align-items:center;flex-wrap:wrap}.table{overflow:auto}table{width:100%;min-width:900px}`],changeDetection:ChangeDetectionStrategy.OnPush})export class StockBalanceComponent{readonly cols=['product','warehouse','location','onHand','reserved','available','value'];readonly items=signal<Balance[]>([]);readonly products=signal<ProductOption[]>([]);readonly warehouses=signal<WarehouseOption[]>([]);readonly total=signal(0);readonly loading=signal(false);readonly search=new FormControl('',{nonNullable:true});readonly warehouse=new FormControl('',{nonNullable:true});readonly product=new FormControl('',{nonNullable:true});page=1;size=20;constructor(private readonly api:InventoryApiService){api.products().subscribe(x=>this.products.set(x.items));api.warehouses().subscribe(x=>this.warehouses.set(x));this.load()}load(){this.loading.set(true);this.api.balances({search:this.search.value||undefined,warehouseId:this.warehouse.value||undefined,productId:this.product.value||undefined,pageNumber:this.page,pageSize:this.size}).subscribe(x=>{this.items.set(x.items);this.total.set(x.totalCount);this.loading.set(false)})}paged(e:PageEvent){this.page=e.pageIndex+1;this.size=e.pageSize;this.load()}export(){this.api.export(this.search.value||undefined,this.warehouse.value||undefined,this.product.value||undefined).subscribe(blob=>{const url=URL.createObjectURL(blob);const anchor=document.createElement('a');anchor.href=url;anchor.download='inventory-balances.xlsx';anchor.click();URL.revokeObjectURL(url)})}}
