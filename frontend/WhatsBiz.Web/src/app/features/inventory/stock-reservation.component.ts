import{ChangeDetectionStrategy,Component,signal}from'@angular/core';import{FormBuilder,ReactiveFormsModule,Validators}from'@angular/forms';import{MatButtonModule}from'@angular/material/button';import{MatFormFieldModule}from'@angular/material/form-field';import{MatInputModule}from'@angular/material/input';import{MatSelectModule}from'@angular/material/select';import{MatSnackBar}from'@angular/material/snack-bar';import{InventoryApiService}from'./inventory-api.service';import{ProductOption,Reservation,WarehouseOption}from'./inventory.models';
@Component({imports:[ReactiveFormsModule,MatButtonModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<h1>Stock Reservation</h1><form [formGroup]="form" (ngSubmit)="save()" class="grid"><mat-form-field><mat-label>Product</mat-label><mat-select formControlName="productId">@for(x of products();track x.productId){<mat-option [value]="x.productId">{{x.productCode}} — {{x.productName}}</mat-option>}</mat-select></mat-form-field><mat-form-field><mat-label>Warehouse</mat-label><mat-select formControlName="warehouseId">@for(x of warehouses();track x.warehouseId){<mat-option [value]="x.warehouseId">{{x.warehouseName}}</mat-option>}</mat-select></mat-form-field><mat-form-field><mat-label>Quantity</mat-label><input matInput type="number" formControlName="quantity"></mat-form-field><mat-form-field><mat-label>Reason</mat-label><input matInput formControlName="reservationReason"></mat-form-field><mat-form-field><mat-label>Reference type</mat-label><input matInput formControlName="referenceType"></mat-form-field><mat-form-field><mat-label>Reference ID</mat-label><input matInput formControlName="referenceId"></mat-form-field><button mat-flat-button type="submit" [disabled]="form.invalid">Reserve stock</button></form><h2>Reservations</h2>@for(x of reservations();track x.stockReservationId){<section><span><strong>{{x.reservationNo}}</strong> · {{x.quantity-x.releasedQuantity}} remaining · {{x.status}} · {{x.reservationReason}}</span>@if(x.status==='ACTIVE'||x.status==='PARTIALLY_RELEASED'){<button mat-button (click)="release(x)">Release remaining</button>}</section>}`,styles:[`.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1rem;max-width:1100px}section{display:flex;justify-content:space-between;align-items:center;padding:.75rem;border-bottom:1px solid var(--mat-sys-outline-variant)}@media(max-width:800px){.grid{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class StockReservationComponent{readonly products=signal<ProductOption[]>([]);readonly warehouses=signal<WarehouseOption[]>([]);readonly reservations=signal<Reservation[]>([]);readonly form;constructor(private readonly api:InventoryApiService,fb:FormBuilder,private readonly snack:MatSnackBar){this.form=fb.group({action:['RESERVE'],stockReservationId:[null],productId:['',Validators.required],warehouseId:['',Validators.required],quantity:[1,Validators.min(.0001)],reservationReason:['',Validators.required],referenceType:[''],referenceId:[null]});api.products().subscribe(x=>this.products.set(x.items));api.warehouses().subscribe(x=>this.warehouses.set(x));this.load()}load(){this.api.reservations().subscribe(x=>this.reservations.set(x))}save(){if(this.form.invalid)return;this.api.reserve(this.form.getRawValue()).subscribe(x=>{this.snack.open(`Reservation ${x.number} created.`,undefined,{duration:3000});this.load()})}release(x:Reservation){this.api.reserve({action:'RELEASE',stockReservationId:x.stockReservationId,quantity:x.quantity-x.releasedQuantity}).subscribe(()=>{this.snack.open('Reservation released.',undefined,{duration:3000});this.load()})}}
