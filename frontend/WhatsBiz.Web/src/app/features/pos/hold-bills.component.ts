import{Component,signal}from'@angular/core';import{MatButtonModule}from'@angular/material/button';import{Router}from'@angular/router';import{POSApiService}from'./pos-api.service';import{InvoiceList}from'./pos.models';
@Component({imports:[MatButtonModule],template:`<h1>Hold Bills</h1>@for(x of bills();track x.invoiceId){<section><span><strong>{{x.invoiceNumber}}</strong> · {{x.invoiceDate}} · {{x.grandTotal}}</span><button mat-flat-button (click)="resume(x)">Resume</button></section>}@empty{<p>No held bills.</p>}`,styles:[`section{display:flex;justify-content:space-between;padding:1rem;border-bottom:1px solid var(--mat-sys-outline-variant)}`]})export class HoldBillsComponent{readonly bills=signal<InvoiceList[]>([]);constructor(private readonly api:POSApiService,private readonly router:Router){api.invoices('HELD').subscribe(x=>this.bills.set(x.items))}resume(x:InvoiceList){this.api.resume(x.invoiceId).subscribe(()=>void this.router.navigate(['/pos'],{queryParams:{resume:x.invoiceId}}))}}
