import{Component,Inject,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{MatButtonModule}from'@angular/material/button';import{MAT_DIALOG_DATA,MatDialogModule,MatDialogRef}from'@angular/material/dialog';import{MatFormFieldModule}from'@angular/material/form-field';import{MatInputModule}from'@angular/material/input';import{MatSelectModule}from'@angular/material/select';import{Payment,PaymentMethod}from'./pos.models';
@Component({imports:[FormsModule,MatButtonModule,MatDialogModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<h2 mat-dialog-title>Payment · {{data.total}}</h2><mat-dialog-content><div class="row"><mat-form-field><mat-label>Method</mat-label><mat-select [(ngModel)]="method">@for(x of data.methods;track x.methodCode){<mat-option [value]="x.methodCode">{{x.methodName}}</mat-option>}</mat-select></mat-form-field><mat-form-field><mat-label>Amount</mat-label><input matInput type="number" [(ngModel)]="amount"></mat-form-field><mat-form-field><mat-label>Reference</mat-label><input matInput [(ngModel)]="reference"></mat-form-field><button mat-button (click)="add()">Add</button></div>@for(x of payments();track $index){<p>{{x.methodCode}}: {{x.amount}}</p>}<p><strong>Paid {{paid()}} · Balance {{data.total-paid()}}</strong></p></mat-dialog-content><mat-dialog-actions><button mat-button mat-dialog-close>Cancel</button><button mat-flat-button [disabled]="paid()<=0||paid()>data.total" (click)="complete()">Complete</button></mat-dialog-actions>`,styles:[`.row{display:flex;gap:1rem;flex-wrap:wrap}`]})export class PaymentDialogComponent{readonly payments=signal<Payment[]>([]);readonly paid=signal(0);method='CASH';amount:number;reference='';constructor(@Inject(MAT_DIALOG_DATA)readonly data:{total:number;methods:PaymentMethod[]},private readonly ref:MatDialogRef<PaymentDialogComponent>){this.amount=data.total}add(){if(this.amount<=0)return;this.payments.update(x=>[...x,{methodCode:this.method,amount:this.amount,referenceNumber:this.reference||undefined}]);this.paid.set(this.payments().reduce((a,b)=>a+b.amount,0));this.amount=Math.max(0,this.data.total-this.paid());this.reference=''}complete(){this.ref.close(this.payments())}}
