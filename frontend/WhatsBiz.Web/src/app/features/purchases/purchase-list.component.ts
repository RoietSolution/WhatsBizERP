import{Component,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{RouterLink}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{MatFormFieldModule}from'@angular/material/form-field';import{MatInputModule}from'@angular/material/input';import{MatSelectModule}from'@angular/material/select';import{PurchaseApiService}from'./purchase-api.service';import{PurchaseList}from'./purchase.models';
@Component({imports:[FormsModule,RouterLink,MatButtonModule,MatFormFieldModule,MatInputModule,MatSelectModule],template:`<header><h1>Purchases</h1><div><button mat-button (click)="export()">Export</button><button mat-button (click)="template()">Excel Template</button><a mat-flat-button routerLink="/purchases/create">Create</a></div></header><div class="filters"><mat-form-field><mat-label>Search</mat-label><input matInput [(ngModel)]="search" (keyup.enter)="load()"></mat-form-field><mat-form-field><mat-label>Status</mat-label><mat-select [(ngModel)]="status" (selectionChange)="load()"><mat-option value="">All</mat-option><mat-option value="DRAFT">Draft</mat-option><mat-option value="POSTED">Posted</mat-option></mat-select></mat-form-field></div><div class="table"><table><thead><tr><th>Number</th><th>Date</th><th>Supplier</th><th>Supplier Invoice</th><th>Total</th><th>Outstanding</th><th>Status</th></tr></thead><tbody>@for(x of rows();track x.purchaseInvoiceId){<tr><td><a [routerLink]="['/purchases',x.purchaseInvoiceId]">{{x.invoiceNumber}}</a></td><td>{{date(x.invoiceDate)}}</td><td>{{x.supplierName}}</td><td>{{x.supplierInvoiceNo}}</td><td>{{x.grandTotal}}</td><td>{{x.balanceAmount}}</td><td><span class="chip">{{x.status}}</span></td></tr>}</tbody></table></div>`,styles:[`header,.filters{display:flex;justify-content:space-between;gap:1rem}.table{overflow:auto}table{width:100%;border-collapse:collapse}th,td{padding:.75rem;border-bottom:1px solid #ddd;text-align:left}.chip{padding:.25rem .5rem;border-radius:1rem;background:#e8eefc}`]})
export class PurchaseListComponent{rows=signal<PurchaseList[]>([]);search='';status='';constructor(private api:PurchaseApiService){this.load()}date(x:string){return x.substring(0,10)}load(){this.api.list(this.search,this.status).subscribe(x=>this.rows.set(x.items))}download(b:Blob,n:string){const u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=n;a.click();URL.revokeObjectURL(u)}export(){this.api.export().subscribe(x=>this.download(x,'purchases.xlsx'))}template(){this.api.template().subscribe(x=>this.download(x,'purchase-template.xlsx'))}}
