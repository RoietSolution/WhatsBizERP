import{Component,signal}from'@angular/core';import{FormsModule}from'@angular/forms';import{ActivatedRoute,Router}from'@angular/router';import{MatButtonModule}from'@angular/material/button';import{MatFormFieldModule}from'@angular/material/form-field';import{MatInputModule}from'@angular/material/input';import{MatSnackBar}from'@angular/material/snack-bar';import{PurchaseApiService}from'./purchase-api.service';import{Purchase}from'./purchase.models';@Component({imports:[FormsModule,MatButtonModule,MatFormFieldModule,MatInputModule],template:`<h1>Purchase Return</h1>@if(p();as x){<p>{{x.invoiceNumber}} · {{x.supplierName}}</p><table><tr><th>Product</th><th>Purchased</th><th>Already returned</th><th>Return now</th></tr>@for(i of x.items;track i.purchaseItemId){<tr><td>{{i.productName}}</td><td>{{i.quantity+i.freeQuantity}}</td><td>{{i.returnedQuantity}}</td><td><input type="number" min="0" [max]="i.quantity+i.freeQuantity-i.returnedQuantity" [(ngModel)]="qty[i.purchaseItemId]"></td></tr>}</table><mat-form-field><mat-label>Return reason</mat-label><textarea matInput [(ngModel)]="reason"></textarea></mat-form-field><button mat-flat-button (click)="submit()">Post Return</button>}`,styles:[`table{width:100%;margin-bottom:1rem}th,td{text-align:left;padding:.6rem}mat-form-field{width:100%}`]})export class PurchaseReturnComponent{p=signal<Purchase|null>(null);qty:Record<string,number>={};reason='';constructor(private api:PurchaseApiService,route:ActivatedRoute,private router:Router,private snack:MatSnackBar){api.get(route.snapshot.paramMap.get('id')!).subscribe(x=>this.p.set(x))}submit(){const x=this.p();if(!x)return;const items=x.items.filter(i=>this.qty[i.purchaseItemId]>0).map(i=>({purchaseItemId:i.purchaseItemId,quantity:this.qty[i.purchaseItemId]}));if(!items.length||!this.reason){this.snack.open('Return quantity and reason are required','Close',{duration:3000});return}this.api.return({purchaseInvoiceId:x.purchaseInvoiceId,items,reason:this.reason}).subscribe(()=>this.router.navigate(['/purchases',x.purchaseInvoiceId]))}}
