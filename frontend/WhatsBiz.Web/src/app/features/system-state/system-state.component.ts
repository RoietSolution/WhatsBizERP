import{ChangeDetectionStrategy,Component,computed,signal}from'@angular/core';import{ActivatedRoute,RouterLink}from'@angular/router';import{MatButtonModule}from'@angular/material/button';
type StateMode='401'|'403'|'404'|'500'|'offline'|'maintenance';
const states:Record<StateMode,{icon:string;eyebrow:string;title:string;description:string;action:string}>={401:{icon:'lock',eyebrow:'Authentication required',title:'Please sign in to continue',description:'Your session is missing or no longer valid. Sign in again to securely access KhataDhari ERP.',action:'Sign In'},403:{icon:'shield_lock',eyebrow:'Access restricted',title:'You do not have permission',description:'Your account is signed in, but this area requires additional permissions from an administrator.',action:'Go to Dashboard'},404:{icon:'travel_explore',eyebrow:'Page not found',title:'We could not find that page',description:'The address may be incorrect, or the page may have moved to another workspace.',action:'Go to Dashboard'},500:{icon:'error',eyebrow:'Something went wrong',title:'The application encountered an error',description:'Your data is safe. Try again, or return to the dashboard while the issue is investigated.',action:'Try Again'},offline:{icon:'cloud_off',eyebrow:'Connection unavailable',title:'You appear to be offline',description:'Check your network connection. KhataDhari ERP will be ready when connectivity returns.',action:'Check Again'},maintenance:{icon:'construction',eyebrow:'Scheduled maintenance',title:'We will be back shortly',description:'KhataDhari ERP is temporarily unavailable while scheduled maintenance is completed.',action:'Check Status'}};
@Component({imports:[RouterLink,MatButtonModule],template:`<main><section><img src="/assets/images/khatadhari-logo.png" alt="KhataDhari ERP"><div class="state-icon"><span class="material-symbols-rounded">{{state().icon}}</span></div><p class="eyebrow">{{state().eyebrow}}</p><h1>{{state().title}}</h1><p class="description">{{state().description}}</p><div class="actions">@if(mode()==='401'){<a mat-flat-button color="primary" routerLink="/login">{{state().action}}</a>}@else if(mode()==='500'||mode()==='offline'||mode()==='maintenance'){<button mat-flat-button color="primary" (click)="retry()">{{state().action}}</button>}@else{<a mat-flat-button color="primary" routerLink="/dashboard">{{state().action}}</a>}<button mat-button type="button" (click)="back()">Go Back</button></div><small>Error {{mode().toUpperCase()}} · KhataDhari ERP</small></section></main>`,styles:[`main{display:grid;min-height:100dvh;padding:24px;background:var(--wb-background);place-items:center}section{width:min(520px,100%);padding:40px;text-align:center;background:var(--wb-surface);border:1px solid var(--wb-border);border-radius:var(--wb-radius-lg);box-shadow:var(--wb-shadow-md);animation:wb-slide-up var(--wb-motion)}img{width:190px;margin-bottom:30px}.state-icon{display:grid;width:72px;height:72px;margin:0 auto 18px;color:var(--wb-primary);background:var(--wb-primary-soft);border-radius:22px;place-items:center}.state-icon .material-symbols-rounded{font-size:38px}.eyebrow{margin:0;color:var(--wb-primary);font-size:12px;font-weight:700;letter-spacing:.08em;text-transform:uppercase}h1{margin:8px 0 10px;font-size:28px}.description{margin:0 auto 24px;color:var(--wb-text-secondary);line-height:1.6}.actions{display:flex;justify-content:center;gap:8px}small{display:block;margin-top:28px;color:var(--wb-text-secondary)}@media(max-width:600px){main{padding:12px}section{padding:28px 20px}h1{font-size:23px}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class SystemStateComponent{readonly mode=signal<StateMode>('404');readonly state=computed(()=>states[this.mode()]);constructor(route:ActivatedRoute){this.mode.set((route.snapshot.data['mode']??'404')as StateMode)}retry(){if(this.mode()==='500')location.reload();else if(navigator.onLine)location.assign('/dashboard');else location.reload()}back(){history.back()}}
