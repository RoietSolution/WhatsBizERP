import { ChangeDetectionStrategy, Component, input } from '@angular/core';

@Component({ selector: 'app-summary-card', template: '<article class="summary-card"><div class="summary-card__icon" [attr.data-tone]="tone()"><span class="material-symbols-rounded">{{ icon() }}</span></div><div><p>{{ label() }}</p><strong>{{ value() }}</strong>@if (trend()) { <small [attr.data-positive]="trendPositive()">{{ trend() }}</small> }</div></article>', styleUrl: './summary-card.component.scss', changeDetection: ChangeDetectionStrategy.OnPush })
export class SummaryCardComponent { readonly label = input.required<string>(); readonly value = input.required<string | number>(); readonly icon = input('analytics'); readonly tone = input<'primary'|'success'|'warning'|'danger'|'info'>('primary'); readonly trend = input(''); readonly trendPositive = input(true); }
